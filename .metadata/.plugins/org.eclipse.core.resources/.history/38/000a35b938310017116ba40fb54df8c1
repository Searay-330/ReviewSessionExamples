import java.util.ArrayList;

public class Example1 {
	public double computeAvg(ArrayList<Integer> list){
		double total = 0.0;
		for(int i = 0; i < list.size(); ++i){
			total += list.get(i);
		}
		return total/list.size();
	}

}
